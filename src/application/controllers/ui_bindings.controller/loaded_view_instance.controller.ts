import { Scoped } from '@modules/rdi/attributes';
import { __sprinkelMetaData } from '@modules/rdi/meta';

@Scoped()
export class LoadedViewInstanceController {
  constructor() {
    // Empty
  }
}
__sprinkelMetaData(LoadedViewInstanceController, []);
